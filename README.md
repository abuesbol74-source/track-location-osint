# 🛰️ TRACK-LOCATION-OSINT
### **Advanced Reconnaissance Tool**
Created by: **LAKSAMANA DZU NUR NAIN**

---

## 🛠️ LANGKAH PERSIAPAN

### **1. Pilih Sesuai Perangkat Anda**

* **Untuk Pengguna Termux (Android):**
    ```bash
    pkg update && pkg upgrade -y
    pkg install git curl jq -y
    ```

* **Untuk Pengguna Ubuntu / Kali Linux / Debian:**
    ```bash
    sudo apt update && sudo apt upgrade -y
    sudo apt install git curl jq -y
    ```

---

## 🚀 INSTALASI & EKSEKUSI SCRIPT

Setelah persiapan di atas selesai, jalankan perintah ini:

```bash
git clone https://github.com/abuesbol74-source/track-location-osint.git

cd track-location-osint

chmod +x track_location.sh

./track_location.sh
